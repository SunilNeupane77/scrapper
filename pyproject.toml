[project]
name = "ekantipur-scraper"
version = "0.1.0"
description = "E kantipur Data Scraper"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "imageio-ffmpeg>=0.6.0",
    "playwright>=1.59.0",
]
